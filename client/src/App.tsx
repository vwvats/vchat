import { ChannelContainer, ChannelListContainer } from "./components";

const App = () => {
  return (
    <div>
      <ChannelContainer />
      <ChannelListContainer />
    </div>
  );
};

export default App;
